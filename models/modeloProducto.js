// Clase Producto con el constructor de los datos solicitados
class Producto {
    constructor(codigo, nombre, descripcion, precio) {
        this.codigo = codigo;
        this.nombre = nombre;
        this.descripcion = descripcion;
        this.precio = precio;
    }
}

// Exportar clase Producto
module.exports = Producto;
